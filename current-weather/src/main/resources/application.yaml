server:
  port: 8082

---

spring:
  profiles:
    active: dev
  cloud:
    vault:
      token: 00000000-0000-0000-0000-000000000000
      host: localhost
      port: 8200
      scheme: http
  application:
    name: current-weather
  config:
    import: vault://